============
Installation
============

At the command line::

    $ easy_install django-gitabix

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-gitabix
    $ pip install django-gitabix
